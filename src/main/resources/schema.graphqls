# This will also resolve to string in Decimal format
#scalar Decimal
type Account{
    id: ID!
    name: String!
    description: String!
}

#This represents a single banking transactions
type Transaction{
    id: ID!
    title: String!
    description: String!
    value: String!
}

type Query{
    accounts:[Account]!
}